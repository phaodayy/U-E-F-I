#include "mapper.hpp"
// mapper module removed: all kernel operations now handled by the
// Hypervisor (Ring -1) via PubgHyperCall hypercall interface.
// No vulnerable driver (iqvw64e.sys / kdmapper) is required or used.
